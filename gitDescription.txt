ls			Show what files and folders there are in the current directory
mkdir gitProject	create a new directory called gitProject
touch fileName.txt	create a new file called fileName.txt
move "fileName.txt" "gitProject"	moves the file to the directory
cd gitProject		Goes to the projectfolder
git init		Creates an empty git reposotory
ls -a			Shows ALL the items, also the hidden
git status		Show the status
touch newFileName.txt	create a new file called newFileName.txt
git add --all		Adds all changes, files and directories to the commit
git commit -m "The first commit message"	A git commit with a message

echo A line I would like to add to the file.>>newFileName.txt
cat newFileName.txt	Print the content of the file to the terminal

the repo we just created is a local repo, and not for collaborating yest, but now we go to github.com and we create an empty reposotory

git remote add origin https://github.com/TheDemon12345678910/githubRepoWithActions.git

git remote -v		Gives outputs if done correctly

git checkout -b ＜new-branch＞
The above example simultaneously creates and checks out ＜new-branch＞. The -b option is a convenience flag that tells Git to run git branch before running git checkout ＜new-branch＞.

git checkout ＜branchname＞	switch to the branch
